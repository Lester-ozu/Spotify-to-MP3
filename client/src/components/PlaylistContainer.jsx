function PlaylistContainer({ children }) {

    return(
        <>
            <div className="playlistContainer">
                {children}
            </div>
        </>
    );
}

export default PlaylistContainer;